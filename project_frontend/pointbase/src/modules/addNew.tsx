import React, { Component } from 'react';
import { render } from 'react-dom';

class AddNew extends Component{

render() {
    return(
        <h1>Yay, at least something I still remember :D</h1>
        );
    }
}
export default AddNew